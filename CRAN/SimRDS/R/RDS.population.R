#' The list that specifies the output of the \code{population} function.
#'
#' This list contains the output of the function \code{population} so that it
#' could be used in the sampling examples. Depending on the performance of the
#' computer it could take a considerable amount of time to produce this output.
#'
"RDS.population"
