# R working files
.Rproj.user
.Rhistory
.Ruserdata
.Rproj
.RData

# OS
*.DS_Store

# Large local-only folders
/Meetings
/_snapshots

# Old project clones (preserved in _snapshots/, removed from working tree)
/DWeMSinUK_1
/DWeMSinUK_RL

# Claude Code working state
/.claude

# Audio recordings & Outlook archives (large binaries)
*.m4a
*.olm

# Redundant zips (the unpacked folder is tracked instead)
/paper/OneDrive_1_22-05-2026.zip

# Derived analysis artefacts — regenerable from R scripts.
# Stay on local disk, but not tracked in git history.
/output/*.RData
/output/*.rds
/output/*.RDS
/output/nsum_quick_test/
/output/nsum_corrected/
/output/nsum_complete/
/output/nsum_comprehensive/
/output/archive_*/
/output/figures20250827/
# Other large derived binaries
*.RData
*.rds
# LaTeX build artifacts (regenerable)
*.aux
*.bbl
*.bcf
*.blg
*.log
*.run.xml
*.synctex.gz
*.fls
*.fdb_latexmk
*.qmd.bak

# Microsoft Word lock files (created when a .docx is open)
~$*.docx
~$*.doc
~$*.xlsx
~$*.pptx
